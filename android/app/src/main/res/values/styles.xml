<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark theme matching The Rabid Vault design -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">#8b5cf6</item>
        <item name="colorPrimaryDark">#6d28d9</item>
        <item name="colorAccent">#a78bfa</item>
        <item name="android:windowBackground">#0d0d12</item>
    </style>

    <!-- Splash screen style - shown before app loads -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">#0d0d12</item>
        <item name="windowSplashScreenBackground">#0d0d12</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
